User {{ joining_user.username }} wants to join Clusterify project "{{ project.title }}".

As the project author, you can approve this by going on the project's page and clicking "approve" besides the user's name in the right sidebar:

http://www.clusterify.com{{ project.get_absolute_url }}
